# Shared deps for data-heavy Lambdas (player_list, details_chunk, reports_chunk, merge_chunks, validate)
# Built once, used by 5 functions
requests>=2.32.5
beautifulsoup4>=4.14.3
pandas>=2.0.0
pyarrow>=14.0.0
tqdm>=4.66.0
